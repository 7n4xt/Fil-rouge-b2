# Copiez ce fichier vers .env et adaptez les valeurs (ne commitez pas .env).

DB_HOST=127.0.0.1
DB_NAME=real_estate
DB_USERNAME=root
# Laissez vide si pas de mot de passe en local (obligatoire : la ligne doit exister)
DB_PASSWORD=
